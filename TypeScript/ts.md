## TypeScript tutorial #3: Project File Structure

```bash
project/
index.ts
tsconfig.json
```

In this file structure example, we have a directory called 'project' that contains two files:

1.`index.ts`
2. `tsconfig.json`
